package pl.marrod.localmark.ui.theme

import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.graphics.Brush
import androidx.compose.ui.graphics.Color

// ── Primary ──────────────────────────────────────────────────────────────────
val Primary                 = Color(0xFFC3F5FF)
val OnPrimary               = Color(0xFF00363D)
val PrimaryContainer        = Color(0xFF00E5FF)
val OnPrimaryContainer      = Color(0xFF00626E)
val PrimaryFixed            = Color(0xFF9CF0FF)
val PrimaryFixedDim         = Color(0xFF00BCD4)
val OnPrimaryFixed          = Color(0xFF001F24)
val OnPrimaryFixedVariant   = Color(0xFF004F58)
val InversePrimary          = Color(0xFF006875)

// ── Secondary ─────────────────────────────────────────────────────────────────
val Secondary               = Color(0xFF6FD5E4)
val OnSecondary             = Color(0xFF00363C)
val SecondaryContainer      = Color(0xFF2E9EAD)
val OnSecondaryContainer    = Color(0xFF002F34)
val SecondaryFixed          = Color(0xFF94F1FF)
val SecondaryFixedDim       = Color(0xFF6FD5E4)
val OnSecondaryFixed        = Color(0xFF001F24)
val OnSecondaryFixedVariant = Color(0xFF004F57)

// ── Tertiary ──────────────────────────────────────────────────────────────────
val Tertiary                = Color(0xFFFFE7E2)
val OnTertiary              = Color(0xFF621100)
val TertiaryContainer       = Color(0xFFFFC2B3)
val OnTertiaryContainer     = Color(0xFFAA2600)
val TertiaryFixed           = Color(0xFFFFDAD2)
val TertiaryFixedDim        = Color(0xFFFFB4A2)
val OnTertiaryFixed         = Color(0xFF3C0700)
val OnTertiaryFixedVariant  = Color(0xFF8A1D00)

// ── Error ─────────────────────────────────────────────────────────────────────
val Error                   = Color(0xFFE91E63)
val OnError                 = Color(0xFF690005)
val ErrorContainer          = Color(0xFF93000A)
val OnErrorContainer        = Color(0xFFFFDAD6)

// ── Background / Surface ──────────────────────────────────────────────────────
val Background              = Color(0xFF131313)
val OnBackground            = Color(0xFFE5E2E1)
val Surface                 = Color(0xFF131313)
val OnSurface               = Color(0xFFE5E2E1)
val SurfaceDim              = Color(0xFF131313)
val SurfaceBright           = Color(0xFF393939)
val SurfaceContainerLowest  = Color(0xFF0E0E0E)
val SurfaceContainerLow     = Color(0xFF1C1B1B)
val SurfaceContainer        = Color(0xFF201F1F)
val SurfaceContainerHigh    = Color(0xFF2A2A2A)
val SurfaceContainerHighest = Color(0xFF353534)
val SurfaceVariant          = Color(0xFF353534)
val SurfaceTint             = Color(0xFF00DAF3)
val InverseSurface          = Color(0xFFE5E2E1)
val InverseOnSurface        = Color(0xFF313030)

// ── Outline ───────────────────────────────────────────────────────────────────
val Outline                 = Color(0xFF849396)
val OutlineVariant          = Color(0xFF3B494C)
val OnSurfaceVariant        = Color(0xFFBAC9CC)

// ════════════════════════════════════════════════════════════════════════════
// Light theme palette
// ════════════════════════════════════════════════════════════════════════════

// ── Primary (light) ──────────────────────────────────────────────────────────
val LightPrimary                 = Color(0xFF006970)
val LightOnPrimary               = Color(0xFFFFFFFF)
val LightPrimaryContainer        = Color(0xFF00becc)
val LightOnPrimaryContainer      = Color(0xFF006970)
val LightPrimaryFixed            = Color(0xFF7DF4FF)
val LightPrimaryFixedDim         = Color(0xFF00DBE9)
val LightOnPrimaryFixed          = Color(0xFF002022)
val LightOnPrimaryFixedVariant   = Color(0xFF004F54)
val LightInversePrimary          = Color(0xFF00DBE9)

// ── Secondary (light) ────────────────────────────────────────────────────────
val LightSecondary               = Color(0xFF40646A)
val LightOnSecondary             = Color(0xFFFFFFFF)
val LightSecondaryContainer      = Color(0xFFC0E7ED)
val LightOnSecondaryContainer    = Color(0xFF44686E)
val LightSecondaryFixed          = Color(0xFFC3E9F0)
val LightSecondaryFixedDim       = Color(0xFFA7CDD3)
val LightOnSecondaryFixed        = Color(0xFF001F23)
val LightOnSecondaryFixedVariant = Color(0xFF274C51)

// ── Tertiary (light) ─────────────────────────────────────────────────────────
val LightTertiary                = Color(0xFF505F76)
val LightOnTertiary              = Color(0xFFFFFFFF)
val LightTertiaryContainer       = Color(0xFFCADBF5)
val LightOnTertiaryContainer     = Color(0xFF506076)
val LightTertiaryFixed           = Color(0xFFD3E4FE)
val LightTertiaryFixedDim        = Color(0xFFB7C8E1)
val LightOnTertiaryFixed         = Color(0xFF0B1C30)
val LightOnTertiaryFixedVariant  = Color(0xFF38485D)

// ── Error (light) ────────────────────────────────────────────────────────────
val LightError                   = Color(0xFFBA1A1A)
val LightOnError                 = Color(0xFFFFFFFF)
val LightErrorContainer          = Color(0xFFFFDAD6)
val LightOnErrorContainer        = Color(0xFF93000A)

// ── Background / Surface (light) ─────────────────────────────────────────────
val LightBackground              = Color(0xFFF7F9FB)
val LightOnBackground            = Color(0xFF191C1E)
val LightSurface                 = Color(0xFFF7F9FB)
val LightOnSurface               = Color(0xFF191C1E)
val LightSurfaceDim              = Color(0xFFD8DADC)
val LightSurfaceBright           = Color(0xFFF7F9FB)
val LightSurfaceContainerLowest  = Color(0xFFFFFFFF)
val LightSurfaceContainerLow     = Color(0xFFF2F4F6)
val LightSurfaceContainer        = Color(0xFFECEEF0)
val LightSurfaceContainerHigh    = Color(0xFFE6E8EA)
val LightSurfaceContainerHighest = Color(0xFFE0E3E5)
val LightSurfaceVariant          = Color(0xFFE0E3E5)
val LightSurfaceTint             = Color(0xFF006970)
val LightInverseSurface          = Color(0xFF2D3133)
val LightInverseOnSurface        = Color(0xFFEFF1F3)

// ── Outline (light) ──────────────────────────────────────────────────────────
val LightOutline                 = Color(0xFF6A7A7B)
val LightOutlineVariant          = Color(0xFFB9CACB)
val LightOnSurfaceVariant        = Color(0xFF3B494B)

// ════════════════════════════════════════════════════════════════════════════
// Glass / shadow effect tokens
//   .glass-panel  → background: rgba(30,30,30,0.8)  backdrop-filter: blur(20px)
//   .inner-glow   → box-shadow: inset 0 1px 0 rgba(255,255,255,0.1)
//   .ambient-shadow → box-shadow: 0 12px 32px rgba(0,0,0,0.4)
// ════════════════════════════════════════════════════════════════════════════

/** rgba(30, 30, 30, 0.80) → alpha 0xCC */
val GlassPanelColor    = OnPrimary.copy(alpha = 0.50f)

/** rgba(255, 255, 255, 0.10) → alpha 0x1A */
val InnerGlowColor     = Color(0x1AFFFFFF)

/** rgba(0, 0, 0, 0.40) → alpha 0x66 */
val AmbientShadowColor = Color(0x66000000)

// ════════════════════════════════════════════════════════════════════════════
// Map background – replicates the CSS dot-grid pattern:
//   background-color: #1a1a1a
//   background-image: radial-gradient(#333 1px, transparent 1px)
//   background-size: 20px 20px
// ════════════════════════════════════════════════════════════════════════════

/** Solid fill for the map canvas background. */
val MapBgColor  = Color(0xFF1A1A1A)

/** Dot color used by [MapBgBrush]. */
val MapDotColor = Color(0xFF333333)

val MapRadialGradientBrush : Brush = Brush.radialGradient(
    colors = listOf(MapDotColor, Color.Transparent),
    center = Offset.Unspecified,
    radius = Float.POSITIVE_INFINITY
)
